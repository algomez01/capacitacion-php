<form method="post" action="{{ path('app_libros_delete', {'id': libro.id}) }}" 
onsubmit="return confirm('¿Estás seguro de eliminar este libro?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ libro.id) }}">
    <button class="btn btn-danger">Eliminar libro</button>
</form>
